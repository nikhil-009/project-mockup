import { CustomClipboardDirective } from './custom-clipboard.directive';

describe('CustomClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
